#!/usr/bin/env bash

source /Users/${USER}/.dotfiles/shell/.functions

# The "git domo/fomo" commands will put you on the correct branch regardless of
# your current branch, so there is no need to check for a feature branch.

# parts=(`echo $(git symbolic-ref -q HEAD) | tr '/' ' '`)

# if [ ${#parts[@]} -lt 4 ]; then
#     abort "You do not appear to be within a feature branch."
# fi

# if [ ${parts[2]} != 'feature' ]; then
#     abort "You do not appear to be within a feature branch."
# fi

if git_branch_exists "develop"; then
    git domo && git gone
else
    git fomo && git gone
fi
