#!/usr/bin/env bash

source "${HOME}/.dotfiles/lib/config.sh"

if ! laravel_application_root; then
    abort "You are not in the root of a Laravel application."
fi

info "Executing 'composer app:verify:fix'"
composer app:verify:fix
