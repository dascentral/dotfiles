#!/usr/bin/env bash

source "${HOME}/.dotfiles/lib/config.sh"

abort 'Command disabled.';

# Remove all containers
#docker rm $(docker ps -a -q)

# Remove all images
#docker rmi $(docker images -q)

# Remove unused volumes
#docker volume prune --force
