# Aliases - Managing this file
alias va='subl ~/.dotfiles/shell/.aliases'

# Directory Switching
alias dev='cd ~/Code'
alias docs='cd ~/Documents'
alias dot='cd ~/.dotfiles'
alias icloud='cd ~/Library/Mobile\ Documents/com~apple~CloudDocs'

# Composer
alias cda='composer dumpautoload'
alias cf='composer format'

# Docker - yes, Docker is the best, but it's slow, and I rarely use it in my work
#alias d='docker'
#alias dc='docker-compose'
#alias di='docker images'
#alias dm='docker-machine'
#alias dv='docker volume ls'

# Git
alias glog="git log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit"
alias gs='git status'
alias gsa='git stash apply'
alias nah='git reset HEAD --hard'
alias repush='git pull --rebase && git push'

# HubFlow
alias hfs='git hf feature start'
alias ghu='git hf update'
alias rmaj='hf release major'
alias rmin='hf release minor'
alias rpat='hf release patch'

# Homebrew
alias bsl='brew services list'
alias bsr='brew services restart'

# Laravel
alias d='~/.dotfiles/bin/dusk'
alias sail='[ -f sail ] && sh sail || sh vendor/bin/sail'
alias td='php artisan test --parallel && php artisan dusk'
alias tf='php artisan test --parallel --stop-on-failure'

# NPM
alias nr='npm run'
alias nrb='npm run build'
alias nrd='npm run dev'
alias nrl='npm run lint'
alias nrf='npm run format'
alias nrs='npm run storybook'

# Storybook
alias storybook='npm run storybook'

# Opening files and folders
alias o='open .'
alias oc='cursor .'
alias or='open README.md'
alias s='subl .'
alias vsc='code .'

# Permissions
alias d755='find . -type d -exec chmod 755 {} \;'
alias f644='find . -type f -exec chmod 644 {} \;'

# Utilities
alias c='clear'
alias clear_ds_store='rm -rf `find . -name '.DS_Store'`'
alias flush_dns='sudo killall -HUP mDNSResponder'
alias fn='find . -name'
alias h='history'
alias hg='history | grep'
alias hosts='sudo vim /etc/hosts'
alias l='ls -al'
alias mc='mc -a -c'
alias myip='curl http://ipecho.net/plain; echo'
alias size='du -hs ./'
alias sshkey='pbcopy < ~/.ssh/id_rsa.pub'

# Zsh Config
alias cz='cat ~/.zshrc'
alias vz='subl ~/.zshrc'
alias sz='source ~/.zshrc'
