#!/usr/bin/env bash

source /Users/${USER}/.dotfiles/shell/.functions

# Recent attempts to automatically configure system settings through the use
# of the ".macos" file results in some system instability. I would not
# recommend the use of this command without an exhaustive evaluation.

# macOS settings
# source .macos
